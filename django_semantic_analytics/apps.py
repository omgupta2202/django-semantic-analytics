from django.apps import AppConfig

class DjangoSemanticAnalyticsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'django_semantic_analytics'
    verbose_name = 'Semantic Analytics'
